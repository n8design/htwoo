import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOORadioButton from './HOORadioButton';

<Meta title="Components/Inputs/HOORadioButton" component={HOORadioButton} />

# HOORadioButton


## Arguments

<ArgTypes of={HOORadioButton} />

export const Template = (args) => <HOORadioButton {...args}/>

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { label: "My RadioButton" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { label: "My RadioButton",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

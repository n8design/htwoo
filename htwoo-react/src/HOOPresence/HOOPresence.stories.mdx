import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOPresence, {HOOPresenceStatus} from './HOOPresence';

<Meta title="Components/Personas/HOOPresence" component={HOOPresence} />

# HOOPresence

Predefined enum defined for the supported avatar presence:
```ts
export enum HOOPresenceStatus {
  Away = "away",
  DoNotDisturb = "dnd",
  Online = "online",
  Invisible = "invisible",
  OutOfOffice = "off"
}
```

## Arguments

<ArgTypes of={HOOPresence} />

export const Template = (args) => <HOOPresence {...args}/>

## Online
<Canvas>
  <Story 
    name="Online"
    args = { 
      { status: HOOPresenceStatus.Online }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { status: HOOPresenceStatus.Online,
        rootElementAttributes: { style: { backgroundColor: "purple" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/blocks';
import { useArgs } from "@storybook/client-api";
import HOOVerticalNav from './HOOVerticalNav';
import { symset } from '../SymbolSet';
import * as HooVerticalNavStories from './HOOVerticalNav.stories';

<Meta of={HooVerticalNavStories} />

[![Available in v1.6](https://img.shields.io/badge/💦%20Added%20Version-1.6-blue.svg?style=for-the-badge)](https://github.com/n8design/htwoo/blob/main/REACT-CHANGELOG.md)

# HOOVerticalNav

## Arguments

<ArgTypes of={HOOVerticalNav} />

export const childNavItems = [{ key: 101, text: "Value 1 - 1", href: "https://microsoft.com" }, { key: 102, text: "Value 1 - 2", onItemClick: (key) => {alert(`Key: ${key}`)} }, { key: 103, text: "Value 1 - 3", href: "https://boston.com" }];
export const navItems = [{ key: 1, text: "Value 1", href: "https://google.com", childNavItems: childNavItems  }, { key: 2, text: "Value 2" }, { key: 3, text: "Value 3", onItemClick: (key) => {alert(`Key: ${key}`)} }];

export const Template = (args) => {
  const [_, updateArgs] = useArgs();

  const handle = (ev, key) => {
    args.selectedKey = key;
    updateArgs({ ...args });
  };

  return <HOOVerticalNav selectedKey={101} {...args}/>;
  //return <HOOVerticalNav selectedKey={101} {...args} onClick={handle}/>;
};

## Standard

<Canvas>
  <Story of={HooVerticalNavStories.Standard} />
</Canvas>

## Expanded on Load

<Canvas>
  <Story of={HooVerticalNavStories.ExpandedOnLoad} />
</Canvas>

## Extending

<Canvas>
  <Story of={HooVerticalNavStories.Extending} />
</Canvas>

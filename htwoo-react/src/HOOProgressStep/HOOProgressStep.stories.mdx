import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOProgressStep from './HOOProgressStep';

<Meta title="Components/Output/HOOProgressStep" component={HOOProgressStep} />

# HOOProgressStep


## Arguments

<ArgTypes of={HOOProgressStep} />

export const Template = (args) => <HOOProgressStep {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { label: "Step 1", offsetPercent: "25" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { label: "Step 1", offsetPercent: "25",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOAccordion from './HOOAccordion';
import HOOLabel from '../HOOLabel';

<Meta title="Components/Accordion/HOOAccordion" component={HOOAccordion} />

# HOOAccordion

Accordion element that expands when the user clicks on the header. If the icon is present it is rotated 90deg when item is expanded.

## Arguments

<ArgsTable of={HOOAccordion} />

export const Template = (args) => <HOOAccordion {...args} />;
export const TemplateChildren = (args) => <HOOAccordion {...args}><HOOLabel label="My Child Content"/></HOOAccordion>;

## Simple
<Canvas>
  <Story 
    name="Simple"
    args = { 
      { header: "My Accordion", iconName: "hoo-icon-arrow-right", content: "My Accordion details."}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Content Children
<Canvas>
  <Story 
    name="Content w/ Children Elements"
    args = { 
      { header: "My Accordion", iconName: "hoo-icon-arrow-right" }
    }>
    {TemplateChildren.bind({})}
  </Story>
</Canvas>

## No Icon
<Canvas>
  <Story 
    name="No Icon"
    args = { 
      { header: "My Accordion", content: "My Accordion details."}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { header: "My Accordion", iconName: "hoo-icon-arrow-right", content: "My Accordion details.", rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
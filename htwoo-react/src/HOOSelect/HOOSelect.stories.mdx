import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOSelect, {
  IHOOSelectOption
} from './HOOSelect';

<Meta title="Components/Inputs/HOOSelect" component={HOOSelect} />

# HOOSelect

## Item Interface
The `options` property should be and array of items of type `IHOOSelectOption`. See defintions below:

```ts
export interface IHOOSelectOption {
  key: string | number;
  text: string;
}
```

## Arguments

<ArgsTable of={HOOSelect} />

export const DefaultParams = {
  options: [
    { key: "Apple", text: "Apple" },
    { key: "Lime", text: "Lime" },
    { key: "Banana", text: "Banana" },
    { key: "Orange", text: "Orange" },
  ],
  id: "TestSelect", 
  value: null,
  containsTypeAhead: false, 
  onChange: (fieldValue, fieldName) => { alert(`FieldValue: ${fieldValue} & FieldName: ${fieldName}`) } 
};
export const Template = (args) => <HOOSelect {...args}/>

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["rootElementAttributes"] = { style: { backgroundColor: "pink" } };
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOCheckbox from './HOOCheckbox.tsx';

<Meta title="Components/Inputs/HOOCheckbox" component={HOOCheckbox} />

# HOOCheckbox


## Arguments

<ArgTypes of={HOOCheckbox} />

export const Template = (args) => <HOOCheckbox {...args} />;

<Canvas>
  <Story
    name="Standard"
    args={
      { label: "My Checkbox" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disbled"
    args={
      { label: "My Checkbox Disabled", disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/blocks';
import HOOTagList from "./HOOTagList"
import {HOOTagType, HOOTagStyle} from '../HOOTag';
import * as HooTagListStories from './HOOTagList.stories';

<Meta of={HooTagListStories} />

# HOOTagList

[![Available in v1.1](https://img.shields.io/badge/💦%20Added%20Version-1.1-blue.svg?style=for-the-badge)](https://github.com/n8design/htwoo/blob/main/REACT-CHANGELOG.md)

Predefined enum defined for the supported tag types and styles:

```ts
export interface IHOOTag {
  text: string;
  linkUrl?: string;
  linkTarget?: string;
  onClick?: React.MouseEventHandler<HTMLButtonElement>;
}
```

See more information about the properties for the child components:

* [HOOTag](?path=/docs/atoms-meta-hootag--primary-button)

## Arguments

<ArgTypes of={HOOTagList} />

### Primary - Button

<Canvas of={HooTagListStories.PrimaryButton} />

### Primary - Link

<Canvas of={HooTagListStories.PrimaryLink} />

### Static: Standard

<Canvas of={HooTagListStories.StaticStandard} />

### Extending

<Canvas of={HooTagListStories.Extending} />

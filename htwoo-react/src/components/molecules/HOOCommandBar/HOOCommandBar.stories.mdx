import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { symset } from '../../../SymbolSet';
import HOOCommandBar, {HOOCommandBarType} from './HOOCommandBar';
import HOOLabel from '../../atoms/HOOLabel/HOOLabel';

<Meta title="Molecules/Menus/HOOCommandBar" component={HOOCommandBar}/>

# HOOCommandBar

The `options` property requires an array of items of type `IHOOCommandItem`.

```ts
export interface IHOOCommandItem {
  key: number | string;
  text: string;
}
```

## Arguments

<ArgsTable of={HOOCommandBar} />
export const flyoutMenuItems = [{ iconName: 'hoo-icon-plus', label: 'First Element' }, { iconName: 'hoo-icon-plus', label: 'Second Element' }];
export const options = [{ key: 1, text: "Menu 1", flyoutMenuItems: flyoutMenuItems }, { key: 2, text: "Menu 2" , flyoutMenuItems: flyoutMenuItems}, { key: 3, text: "Menu 3" , flyoutMenuItems: flyoutMenuItems}, { key: 4, text: "Menu 4" }, { key: 5, text: "Menu 5", flyoutMenuItems: [] }];
export const DefaultParams = {
  selectedKey: 1, 
  commandItems: options, 
  onClick: () => { alert("Clicked");} 
}
export const Template = (args) => <HOOCommandBar {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      (function () {
        symset.initSymbols();
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Overflow
<Canvas>
  <Story 
    name="Overflow"
    args = { 
      (function () {
        symset.initSymbols();
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["hasOverflow"] = true;
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      (function () {
        symset.initSymbols();
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["rootElementAttributes"] = { style: { backgroundColor: "red" } };
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>
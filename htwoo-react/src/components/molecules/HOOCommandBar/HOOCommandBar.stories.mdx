import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOCommandBar, {HOOCommandBarType} from './HOOCommandBar';
import HOOLabel from '../../atoms/HOOLabel/HOOLabel';

<Meta title="Molecules/Menus/HOOCommandBar" component={HOOCommandBar}/>

# HOOCommandBar

The `options` property requires an array of items of type `IHOOCommandItem`.

```ts
export interface IHOOCommandItem {
  key: number | string;
  text: string;
}
```

## Arguments

<ArgsTable of={HOOCommandBar} />

export const options = [{ key: 1, text: "Menu 1" }, { key: 2, text: "Menu 2" }, { key: 3, text: "Menu 3" }, { key: 4, text: "Menu 4" }, { key: 5, text: "Menu 5" }];
export const Template = (args) => <HOOCommandBar {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { selectedKey: 1, commandItems: options, hasOverflow: false, onClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Overflow
<Canvas>
  <Story 
    name="Overflow"
    args = { 
      { selectedKey: 1, commandItems: options, hasOverflow: true, onClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { selectedKey: 1, commandItems: options, hasOverflow: false, onClick: () => { alert("Clicked"); }, rootElementAttributes: { style: { backgroundColor: "red" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
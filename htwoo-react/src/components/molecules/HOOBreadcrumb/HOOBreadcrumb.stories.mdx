import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOBreadcrumb, {IHOOBreadcrumbItem, HOOBreadcrumbType} from './HOOBreadcrumb';

<Meta title="Molecules/Menus/HOOBreadcrumb" component={HOOBreadcrumb} />

# HOOBreadcrumb


## Arguments

<ArgsTable of={HOOBreadcrumb} />

export const buttonOptions = [{ key: 1, text: "Value 1" }, { key: 2, text: "Value 2" }, { key: 3, text: "Value 3" }];
export const hyperlinkOptions = [{ key: 1, text: "Value 1", href: "#" }, { key: 2, text: "Value 2", href: "#" }, { key: 3, text: "Value 3", href: "#" }];
export const Template = (args) => <HOOBreadcrumb {...args} />;

## Buttons
<Canvas>
  <Story 
    name="Buttons"
    args = { 
      { type: HOOBreadcrumbType.Button, breadcrumbItems: buttonOptions, onBreadcrumbClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Buttons: Seperated
<Canvas>
  <Story 
    name="Buttons: Seperated"
    args = { 
      { type: HOOBreadcrumbType.Button, breadcrumbItems: buttonOptions, seperatorIconName: "hoo-icon-arrow-right", onBreadcrumbClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Hyperlink: Seperated
<Canvas>
  <Story 
    name="Hyperlink: Seperated"
    args = { 
      { type: HOOBreadcrumbType.Hyperlink, breadcrumbItems: hyperlinkOptions, seperatorIconName: "hoo-icon-arrow-right" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { type: HOOBreadcrumbType.Button, breadcrumbItems: buttonOptions, seperatorIconName: "hoo-icon-arrow-right", onBreadcrumbClick: () => { alert("Clicked"); }, rootElementAttributes: { style: { backgroundColor: "red" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOOptionList, {HOOOptionListType} from './HOOOptionList.tsx';
import HOOLabel from '../../atoms/HOOLabel/HOOLabel';

<Meta title="Molecules/Forms/HOOOptionList" component={HOOOptionList}/>

# HOOOptionList

The `options` property requires an array of items of type `IHOOListOption`.

Predefined enum defined for the supported Option list type and the option list direction:
```ts
export enum HOOOptionListType {
  "Checkbox",
  "RadioButton",
}

export enum HOOOptionListDirection {
  "Vertical",
  "Horizontal"
}
```

## Arguments

<ArgsTable of={HOOOptionList} />

export const options = [{ key: 1, text: "Value 1" }, { key: 2, text: "Value 2" }, { key: 3, text: "Value 3" }];
export const Template = (args) => <HOOOptionList {...args} />;

## Checkbox List
<Canvas>
  <Story 
    name="Checkbox List"
    args = { 
      { type: HOOOptionListType.Checkbox, options: options, value: [1] }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Radio Button List
<Canvas>
  <Story 
    name="Radio Button List"
    args = { 
      { type: HOOOptionListType.RadioButton, options: options, value: 1 }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { type: HOOOptionListType.Checkbox, options: options, value: [1], rootElementAttributes: { style: { backgroundColor: "red" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { symset } from '../../../SymbolSet';
import HOOButtonCommand, {
  IHOOButtonCommandProps,
  HOOButtonCommandType
} from './HOOButtonCommand';

<Meta title="Atoms/Buttons/HOOButtonCommand" component={HOOButtonCommand} />

# HOOButtonCommand

## Arguments

<ArgsTable of={HOOButtonCommand} />

export const DefaultParams = {
  label: "Button",
  flyoutContextItems: [{ iconName: 'hoo-icon-plus', label: 'First Element' }, { iconName: 'hoo-icon-plus', label: 'Second Element' }],
  flyoutContextItemsClicked: (event, item) => {
    console.alert(item.label);
  }
}
export const Template = (args) => <HOOButtonCommand {...args} />;

## Standard
<Canvas>
  <Story 
    name="Standard"
    args = { 
      (function () {
        symset.initSymbols();
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## No Flyout Menu
<Canvas>
  <Story 
    name="No Flyout Menu"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["flyoutContextItems"] = [];
        delete mergedArgs["flyoutContextItemsClicked"];
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["type"] = HOOButtonCommandType.Primary;
        mergedArgs["rootElementAttributes"] = { style: { backgroundColor: "red" } };
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOSearch from './HOOSearch';

<Meta title="Atoms/Input/HOOSearch" component={HOOSearch} />

# HOOSearch


## Arguments

<ArgsTable of={HOOSearch} />

export const Template = (args) => <HOOSearch {...args}/>

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { placeholder: "Search here",
        value: "",
        onChange: () => {alert("changed")} }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { placeholder: "Search here",
        value: "",
        onChange: () => {alert("changed")},
        rootElementAttributes: { style: { backgroundColor: "red" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

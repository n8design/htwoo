import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOLabel from './HOOLabel';

<Meta title="Atoms/Input/HOOLabel" component={HOOLabel} />

# HOOLabel


## Arguments

<ArgsTable of={HOOLabel} />

export const Template = (args) => <HOOLabel {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { label: "My Label" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { label: "My Label",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

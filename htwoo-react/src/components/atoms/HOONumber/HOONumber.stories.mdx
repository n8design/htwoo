import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOONumber from './HOONumber';

<Meta title="Atoms/Input/HOONumber" component={HOONumber} />

# HOONumber


## Arguments

<ArgsTable of={HOONumber} />

export const Template = (args) => <HOONumber {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { value: 0 }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled
<Canvas>
  <Story 
    name="Disabled"
    args = { 
      { value: 0, disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { value: 0,
        rootElementAttributes: { style: { backgroundColor: "red" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

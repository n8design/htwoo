import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOPivotButton from './HOOPivotButton';

<Meta title="Atoms/Buttons/HOOPivotButton" component={HOOPivotButton} />

# HOOPivotButton


## Arguments

<ArgsTable of={HOOPivotButton} />

export const Template = (args) => <HOOPivotButton {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { label: "Pivot Button", isActive: true, onClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { label: "Pivot Button", isActive: true, onClick: () => { alert("Clicked"); },
        rootElementAttributes: { style: { backgroundColor: "mintcream" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOCheckbox from './HOOCheckbox.tsx';

<Meta title="Atoms/Input/HOOCheckbox" component={HOOCheckbox} />

# HOOCheckbox


## Arguments

<ArgsTable of={HOOCheckbox} />

export const Template = (args) => <HOOCheckbox {...args} />;

<Canvas>
  <Story
    name="Standard"
    args={
      { label: "My Checkbox" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disbled"
    args={
      { label: "My Checkbox Disabled", disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { symset } from '../../../SymbolSet';
import HOOButtonSplit, {
  IHOOButtonSplitProps,
  HOOButtonSplitType
} from './HOOButtonSplit';

<Meta title="Atoms/Buttons/HOOButtonSplit" component={HOOButtonSplit} />

# HOOButtonSplit

Predefined enum defined for the supported split button types:
```ts
export enum HOOButtonSplitType {
  "Standard",
  "Primary"
}
```

## Arguments

<ArgsTable of={HOOButtonSplit} />

export const DefaultParams = {
  label: "Button",
  flyoutContextItems: [{ iconName: 'hoo-icon-plus', label: 'First Element' }, { iconName: 'hoo-icon-plus', label: 'Second Element' }],
  flyoutContextItemsClicked: (event, item) => {
    console.alert(item.label);
  }
}
export const Template = (args) => <HOOButtonSplit {...args} />;

## Standard
<Canvas>
  <Story 
    name="Standard"
    args = { 
      (function () {
        symset.initSymbols();
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["type"] = HOOButtonSplitType.Standard;
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Primary
<Canvas>
  <Story 
    name="Primary"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["type"] = HOOButtonSplitType.Primary;
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      (function () {
        const mergedArgs = JSON.parse(JSON.stringify(DefaultParams));
        mergedArgs["type"] = HOOButtonSplitType.Primary;
        mergedArgs["rootElementAttributes"] = { style: { backgroundColor: "red" } };
        return mergedArgs;
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOODate from './HOODate';

<Meta title="Atoms/Input/HOODate" component={HOODate} />

# HOODate


## Arguments

<ArgsTable of={HOODate} />

export const Template = (args) => <HOODate {...args} />;

## Standard
<Canvas>
  <Story 
    name="Standard"
    args = { 
      { value: (new Date()).toLocaleDateString() }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## With Min/Max values set
<Canvas>
  <Story 
    name="Min/Max Values"
    args = {
      (function () {
        const _minValue = new Date();
        const _maxValue = new Date();

        _maxValue.setFullYear(_maxValue.getFullYear() + 1);
        _minValue.setFullYear(_minValue.getFullYear() - 1);

        return { value: (new Date()).toLocaleDateString(), minValue: _minValue.toISOString().split('T')[0], maxValue: _maxValue.toISOString().split('T')[0] };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled
<Canvas>
  <Story 
    name="Disabled"
    args = { 
      { value: (new Date()).toLocaleDateString(), disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { value: (new Date()).toLocaleDateString(), rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
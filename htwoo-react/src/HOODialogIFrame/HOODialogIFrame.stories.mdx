import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOODialogIFrame, {HOOIFrameRatio} from './HOODialogIFrame';
import HOOLabel from '../HOOLabel';

<Meta title="Components/Dialogs/HOODialogIFrame" component={HOODialogIFrame}/>

# HOODialogIFrame

Predefined enum defined for the supported IFrame ratios:
```ts
export enum HOOIFrameRatio {
  "3by4" = "3by4",
  "squared" = "squared",
  "16by9" = "16by9"
}
```

## HOODialogContent

See more information about the properties for the parent component:
- [HOODialogContent](?path=/docs/molecules-dialog-hoodialogcontent--basic)

## Arguments

<ArgTypes of={HOODialogIFrame} />

export const Template = (args) => <HOODialogIFrame {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { src: "https://lab.n8d.studio/htwoo/" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## 3:4 Ratio
<Canvas>
  <Story 
    name="3:4 Ratio"
    args = { 
      { src: "https://lab.n8d.studio/htwoo/", ratio: HOOIFrameRatio['3by4'] }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Square Ratio
<Canvas>
  <Story 
    name="Square Ratio"
    args = { 
      { src: "https://lab.n8d.studio/htwoo/", ratio: HOOIFrameRatio.squared }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## 16:9 Ratio
<Canvas>
  <Story 
    name="16:9 Ratio"
    args = { 
      { src: "https://lab.n8d.studio/htwoo/", ratio: HOOIFrameRatio['16by9'] }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { src: "https://lab.n8d.studio/htwoo/", rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
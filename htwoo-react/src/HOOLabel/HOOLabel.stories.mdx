import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOLabel from './HOOLabel';

<Meta title="Components/Inputs/HOOLabel" component={HOOLabel} />

# HOOLabel


## Arguments

<ArgTypes of={HOOLabel} />

export const Template = (args) => <HOOLabel {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { label: "My Label" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { label: "My Label",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

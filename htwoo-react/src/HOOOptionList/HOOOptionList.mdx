import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/blocks';
import HOOOptionList, {HOOOptionListType} from './HOOOptionList.tsx';
import * as HooOptionListStories from './HOOOptionList.stories';

<Meta of={HooOptionListStories} />

# HOOOptionList

The `options` property requires an array of items of type `IHOOListOption`.

Predefined enum defined for the supported Option list type and the option list direction:

```ts
export interface IHOOListOption {
  key: number | string;
  text: string;
}

export enum HOOOptionListType {
  "Checkbox",
  "RadioButton",
}

export enum HOOOptionListDirection {
  "Vertical",
  "Horizontal"
}
```

## Arguments

<ArgTypes of={HOOOptionList} />

## Checkbox List

<Canvas of={HooOptionListStories.CheckboxList} />

## Multi-column Checkbox List

<Canvas of={HooOptionListStories.MultiColumnCheckboxList} />

## Radio Button List

<Canvas of={HooOptionListStories.RadioButtonList} />

## Extending

<Canvas of={HooOptionListStories.Extending} />

import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/blocks';
import HOOWebPartTitle from './HOOWebPartTitle';
import * as HooWebPartTitleStories from './HOOWebPartTitle.stories';

<Meta of={HooWebPartTitleStories} />

# HOOWebPartTitle

## Arguments

<ArgTypes of={HOOWebPartTitle} />

## Basic

<Canvas of={HooWebPartTitleStories.Basic} />

## Extending

<Canvas of={HooWebPartTitleStories.Extending} />

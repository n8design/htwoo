import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOONumber from './HOONumber';

<Meta title="Components/Inputs/HOONumber" component={HOONumber} />

# HOONumber


## Arguments

<ArgTypes of={HOONumber} />

export const Template = (args) => <HOONumber {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { value: 0 }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled
<Canvas>
  <Story 
    name="Disabled"
    args = { 
      { value: 0, disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { value: 0,
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOProgressBar from './HOOProgressBar';

<Meta title="Components/Output/HOOProgressBar" component={HOOProgressBar} />

# HOOProgressBar


## Arguments

<ArgTypes of={HOOProgressBar} />

export const Template = (args) => <HOOProgressBar {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { value: "20" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { value: "20",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

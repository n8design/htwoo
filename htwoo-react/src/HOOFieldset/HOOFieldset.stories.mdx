import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOFieldset, { HOOFieldsetType } from './HOOFieldset';
import HOOField from '../HOOField';
import HOOLabel from '../HOOLabel';
import HOOInputDesc from '../HOOInputDesc';
import HOOText from '../HOOText';
import HOOValidationMsg from '../HOOValidationMsg';

<Meta title="Components/Inputs/HOOFieldset" component={HOOFieldset} />

# HOOFieldset

A HOOFieldset component will create a HOOLegend component and then render all child elements below it.

## Arguments

<ArgTypes of={HOOFieldset} />

export const Template = (args) => <HOOFieldset {...args}>
<HOOField>
  <HOOLabel label='My Field A' />
  <HOOInputDesc description='My field A description' />
  <HOOText value="Temp"/>
  <HOOValidationMsg validationMsg='My Validation Message'/>
</HOOField>
<HOOField>
  <HOOLabel label='My Field B' />
  <HOOInputDesc description='My field B description' />
  <HOOText value="Temp"/>
  <HOOValidationMsg validationMsg='My Validation Message'/>
</HOOField>
</HOOFieldset>;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { legendText: "My Legend" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>


## No Outline
<Canvas>
  <Story 
    name="NoOutline"
    args = { 
      { legendText: "My Legend", type: HOOFieldsetType.NoOutline  }
    }>
    {Template.bind({})}
  </Story>
</Canvas>


## Raised
<Canvas>
  <Story 
    name="Raised"
    args = { 
      { legendText: "My Legend", type: HOOFieldsetType.Raised }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { legendText: "My Legend",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

import { ArgTypes } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import HOOField from './HOOField';
import HOOLabel from '../HOOLabel';
import HOOInputDesc from '../HOOInputDesc';
import HOOText from '../HOOText';
import HOOValidationMsg from '../HOOValidationMsg';
import * as HooFieldStories from './HOOField.stories';

<Meta of={HooFieldStories} />

# HOOField

A HOOField component will isolate the child components passed to it looking for a HOOLabel, HOOInputDesc, and HOOValidationMsg component. All other components will be rendered in the order they're passed in between the HOOInputDesc and HOOValidationMsg components.

## Arguments

<ArgTypes of={HOOField} />

## Basic

<Canvas of={HooFieldStories.Basic} />

## Extending

<Canvas of={HooFieldStories.Extending} />

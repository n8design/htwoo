import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOToggle from './HOOToggle';

<Meta title="Components/Inputs/HOOToggle" component={HOOToggle} />

# HOOToggle


## Arguments

<ArgTypes of={HOOToggle} />

export const Template = (args) => <HOOToggle {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { labelOn: "On", labelOff: "Off" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled
<Canvas>
  <Story 
    name="Disabled"
    args = { 
      { labelOn: "On", labelOff: "Off", disabled: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { labelOn: "On", labelOff: "Off", rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
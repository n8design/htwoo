import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOValidationMsg from './HOOValidationMsg';

<Meta title="Components/Inputs/HOOValidationMsg" component={HOOValidationMsg} />

# HOOValidationMsg


## Arguments

<ArgTypes of={HOOValidationMsg} />

export const Template = (args) => <HOOValidationMsg {...args} />;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      { validationMsg: "My text input validation message" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
      { validationMsg: "My text input validation message",
        rootElementAttributes: { style: { backgroundColor: "pink" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

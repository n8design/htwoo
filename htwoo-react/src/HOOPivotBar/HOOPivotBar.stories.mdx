import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { useArgs } from "@storybook/client-api";
import HOOPivotBar, {HOOPivotBarType} from './HOOPivotBar';
import HOOLabel from '../HOOLabel';

<Meta title="Components/Menus/HOOPivotBar" component={HOOPivotBar}/>

# HOOPivotBar

The `options` property requires an array of items of type `IHOOPivotItem`.

```ts
export interface IHOOPivotItem {
  key: number | string;
  text: string;
}
```

## Arguments

<ArgsTable of={HOOPivotBar} />

export const options = [{ key: 1, text: "Menu 1" }, { key: 2, text: "Menu 2" }, { key: 3, text: "Menu 3" }, { key: 4, text: "Menu 4" }, { key: 5, text: "Menu 5" }];
export const Template = (args) => {
  const [_, updateArgs] = useArgs();

  const handle = (ev, key) => {
    args.selectedKey = key;
    updateArgs({ ...args });
  };

  return <HOOPivotBar {...args} onClick={handle} />;
};

## Basic
<Canvas>
  <Story 
    name="Basic"
    args = { 
      {selectedKey: 1, pivotItems: options, hasOverflow: false}
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Overflow
<Canvas>
  <Story 
    name="Overflow"
    args = { 
      { selectedKey: 1, pivotItems: options, hasOverflow: true }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending
<Canvas>
  <Story 
    name="Extending"
    args = { 
     { selectedKey: 1, pivotItems: options, hasOverflow: false, rootElementAttributes: { style: { backgroundColor: "mintcream" } }}
    }>
    {Template.bind({})}
  </Story>
</Canvas>
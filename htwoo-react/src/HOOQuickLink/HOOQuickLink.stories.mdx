import { ArgTypes } from '@storybook/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { symset } from '../SymbolSet';
import HOOQuickLink, {HOOQuickLinkType, HOOQuickLinkStyle, HOOQuickLinkAlignment, HOOQuickLinkButtonLines, HOOQuickLinkImageSize} from './HOOQuickLink';

<Meta title="Components/Quick Link/HOOQuickLink" component={HOOQuickLink} />

# HOOQuickLink

Predefined enum defined for the supported button types:
```ts
export enum HOOQuickLinkType {
  "Link",
  "Compact",
  "Grid",
  "Button",
  "Tile"
}
```
Style options for the Quick Link Button type component:

```ts
export enum HOOQuickLinkStyle{ 
  "Outline"="",
  "Filled"="filled",
  "None"="no-outline"
}

export enum HOOQuickLinkAlignment{
  "Left"="",
  "Center"="center"
}

export enum HOOQuickLinkButtonLines{
  "Normal"="",
  "One"="one-line",
}
```

Image Size options for teh Quick Link Tile type component:
```ts
export enum HOOQuickLinkImageSize{
  "Small"="",
  "Medium"="img-m",
  "Large"="img-l",
  "XLarge"="img-xl",
  "Fill"="img-fill",
}
```

When the Quick Link element is not going to be hosted in a SharePoint page and you are using this component in conjunction with the `HOOQuickLinkGrid`, you need to add the columnSpan element to tell the grid how many columns the element will span.

## Arguments

<ArgTypes of={HOOQuickLink} />

export const Template = (args) => <HOOQuickLink {...args} />;

## Quick Link - Standard
<Canvas>
  <Story
    name="Quick Link: Standard"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Link, iconName: "hoo-icon-ninjacat", title: 'My Quick Link Item', description: "Details about my quick link item" };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Quick Link - Compact
<Canvas>
  <Story
    name="Quick Link: Compact"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Compact, iconName: "hoo-icon-ninjacat", title: 'My Quick Link Item', description: "Details about my quick link item" };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas> 

## Quick Link - Standard Edit Mode
<Canvas>
  <Story
    name="Quick Link: Standard Edit Mode"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Link, iconName: "hoo-icon-ninjacat", title: 'My Quick Link Item', description: "Details about my quick link item", editMode: true };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Quick Link - Button
<Canvas>
  <Story
    name="Quick Link: Button"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Button, style: HOOQuickLinkStyle.Filled, alignment: HOOQuickLinkAlignment.Center, buttonLines: HOOQuickLinkButtonLines.Normal, iconName: "hoo-icon-ninjacat", title: 'My Quick Link Item', description: "Details about my quick link item" };
      })()      
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Quick Link - Tile
<Canvas>
  <Story
    name="Quick Link: Tile"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Tile, imageSize: HOOQuickLinkImageSize.Large, iconSrc: "https://lab.n8d.studio/htwoo/htwoo-core/images/card-images/coffee-image.jpg", title: 'My Quick Link Item' };
      })()      
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Quick Link - Grid
<Canvas>
  <Story
    name="Quick Link: Grid"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Grid,  iconSrc: "https://lab.n8d.studio/htwoo/htwoo-core/images/card-images/coffee-image.jpg", title: 'My Quick Link Item' };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>

## Extending the Quick Link component
<Canvas>
  <Story
    name="Extending"
    args={
      (function(){
        symset.initSymbols();
        return { type: HOOQuickLinkType.Link, iconName: "hoo-icon-ninjacat", title: 'My Quick Link Item', description: "Details about my quick link item", rootElementAttributes: { style: { backgroundColor: "pink" } } };
      })()
    }>
    {Template.bind({})}
  </Story>
</Canvas>


import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import HOOTag, {HOOTagType, HOOTagStyle} from './HOOTag';

<Meta title="Components/Meta/HOOTag" component={HOOTag} />

# HOOTag
[![Available in v1.1](https://img.shields.io/badge/💦%20Added%20Version-1.1-blue.svg?style=for-the-badge)](https://github.com/n8design/htwoo/blob/main/REACT-CHANGELOG.md)

Predefined enum defined for the supported tag types and styles:
```ts
export enum HOOTagType {
  Button = "button",
  Link = "link",
  Static = "static"
}

export enum HOOTagStyle {
  Primary = "primary",
  Standard = "standard"
}
```

## Arguments

<ArgsTable of={HOOTag} />

export const Template = (args) => <HOOTag {...args} />;

### Primary - Button
<Canvas>
  <Story
    name="Primary: Button"
    args={
      { tagType: HOOTagType.Button , tagStyle: HOOTagStyle.Primary, text: 'Tag Button', onClick: () => { alert("Clicked"); } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

### Primary - Link
<Canvas>
  <Story
    name="Primary: Link"
    args={
      { tagType: HOOTagType.Link , tagStyle: HOOTagStyle.Primary, text: 'Tag Link', linkUrl: "https://google.com", linkTarget: "_blank" }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

### Static: Standard
<Canvas>
  <Story
    name="Static: Standard"
    args={
      { tagType: HOOTagType.Static , tagStyle: HOOTagStyle.Standard, text: 'Tag Static' }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

### Extending
<Canvas>
  <Story
    name="Extending"
    args={
      { tagType: HOOTagType.Static , tagStyle: HOOTagStyle.Standard, text: 'Tag Static Ext', rootElementAttributes: { style: { backgroundColor: "pink" } } }
    }>
    {Template.bind({})}
  </Story>
</Canvas>

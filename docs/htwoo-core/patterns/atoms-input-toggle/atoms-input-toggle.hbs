<div class="hoo-toggle">
    <input type="checkbox" class="hoo-toggle-cb" name="{{ toggleName }}" id="{{ toggleID }}"><label for="{{ toggleID }}" class="hoo-toggle-label"><span class="hoo-toggle-slider"></span><span class="hoo-toggle-checked">{{ labelChecked }}</span><span class="hoo-toggle-unchecked">{{ labelUnchecked }}</span></label>
</div>
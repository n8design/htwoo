{{#if aria-label }}
<button class="lqd-buttonicon" aria-label="{{ aria-label }}">
    {{ else }}
    <button class="lqd-buttonicon" aria-label="{{ aria-label }}">
        {{/if}}
        {{> atoms-icon iconname=iconAction }}
    </button>